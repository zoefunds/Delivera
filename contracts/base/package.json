{
  "name": "delivera-contracts-base",
  "version": "1.0.0",
  "private": true,
  "description": "DeliveraEscrow — USDC escrow contract for Base Sepolia, paired with GenLayer's contracts/delivera.py",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10"
  }
}
